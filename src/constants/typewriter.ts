/**
 * Typewriter effect constants
 */

/** Delay between each character in the typewriter effect (milliseconds) */
export const TYPEWRITER_DELAY_MS = 45;

/** Interval for audio playback during typing (milliseconds) - synchronized with typing speed */
export const AUDIO_PLAY_INTERVAL_MS = 90;

/** Interval for speaker icon animation during typing (milliseconds) */
export const ICON_ANIMATION_INTERVAL_MS = 150;

/** Duration of the clear effect animation (milliseconds) */
export const CLEAR_EFFECT_DURATION_MS = 400;

/** Delay before starting to type when switching to a new message group (milliseconds) */
export const NEW_GROUP_DELAY_MS = 300;

/** Delay before starting to type within the same message group (milliseconds) */
export const SAME_GROUP_DELAY_MS = 200;
